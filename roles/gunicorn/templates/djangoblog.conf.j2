description "djangoblog"

start on (filesystem)
stop on runlevel [016]

respawn
setuid nobody
setgid nogroup
chdir {{ django_home }}/imagersite
exec export DEBUG=False
exec gunicorn -b 127.0.0.1:8000 imagersite.wsgi
